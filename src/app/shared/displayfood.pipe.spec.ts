import { DisplayfoodPipe } from './displayfood.pipe';

describe('DisplayfoodPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayfoodPipe();
    expect(pipe).toBeTruthy();
  });
});
